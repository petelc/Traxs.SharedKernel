name: Publish NuGet Package

on:
  release:
    types: [published]

env:
  PROJECT_FILE: Traxs.SharedKernel/src/Traxs.SharedKernel/Traxs.SharedKernel.csproj
  TEST_PROJECT: Traxs.SharedKernel/tests/Traxs.SharedKernel.UnitTests/Traxs.SharedKernel.UnitTests.csproj
  PACK_OUTPUT: artifacts/package/release

jobs:
  publish:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '9.0.x'

      - name: Configure NuGet credentials
        run: |
          dotnet nuget remove source github || true
          dotnet nuget add source --username petelc --password ${{ secrets.GITHUB_TOKEN }} --store-password-in-clear-text --name github "https://nuget.pkg.github.com/petelc/index.json"

      - name: Restore dependencies
        run: dotnet restore

      - name: Build
        run: dotnet build "${{ env.PROJECT_FILE }}" --configuration Release --no-restore

      - name: Test
        run: dotnet test "${{ env.TEST_PROJECT }}" --configuration Release --no-build --no-restore

      - name: Pack
        run: dotnet pack "${{ env.PROJECT_FILE }}" --configuration Release --no-build --no-restore --output "${{ env.PACK_OUTPUT }}"

      - name: Publish to GitHub Packages
        run: dotnet nuget push "${{ env.PACK_OUTPUT }}/*.nupkg" --api-key ${{ secrets.GITHUB_TOKEN }} --source https://nuget.pkg.github.com/petelc/index.json --skip-duplicate
